apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: training
spec:
  selector:
    app.kubernetes.io/name: redis
  #type: ClusterIP
  #clusterIP: None
  ports:
  - port: 6379
    targetPort: redis
 